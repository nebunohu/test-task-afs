
import React, { FC } from 'react';

import building from '../../images/Building.svg';

const BuildingIcon: FC = () => {
  return (
    <img src={building} alt="" />
  );
};

export default BuildingIcon;
