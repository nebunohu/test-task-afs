import React, { FC } from 'react';

import edit from '../../images/Edit.svg';

const EditIcon: FC = () => {
  return (
    <img style={{display: 'block'}} src={edit} alt="" />
  );
};

export default EditIcon;